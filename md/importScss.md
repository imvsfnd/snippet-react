# How to use scss in React
1.引入同名class名的css名稱

JSX
<code>

    import './MyButton.css';    

    const MyButton = ({
        <button className='btn'>{children}</button>
    });

    export default MyButton;

</code>